import { action } from '@storybook/addon-actions';
import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks';
import Form from '../Form';
import SubmitButton from '../SubmitButton';
import useFormSubmittedValues from '../useFormSubmittedValues';
import PasswordField from './PasswordField';
import FormDemo from '../shared/FormDemo';

export const initialValues = { password: 'abc123%' };

<Meta title="Password Field" component={PasswordField} />

Password field with a toggle to show/hide the typed password

<Preview>
  <Story name="basic">
    <FormDemo onSubmit={action('submitted')} initialValues={initialValues}>
      <PasswordField name="password" label="Password:" />
    </FormDemo>
  </Story>
</Preview>

## Props

<Props of={PasswordField} />
